# Codeception Test Suite Configuration
#
# Suite for acceptance tests.

actor: AcceptanceTester

modules:
  enabled:
    - Asserts
    - Helper\ConfigHelper
    - Helper\Acceptance
    - Helper\ExtHelper

some_param: 42
